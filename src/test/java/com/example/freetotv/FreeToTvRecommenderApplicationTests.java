package com.example.freetotv;

import org.junit.jupiter.api.Test;
import org.springframework.boot.test.context.SpringBootTest;

@SpringBootTest
class FreeToTvRecommenderApplicationTests {

    @Test
    void contextLoads() {
        // Verifies the full application context (beans, config properties, caching) starts cleanly.
    }
}
